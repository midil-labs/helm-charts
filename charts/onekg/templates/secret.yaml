apiVersion: v1
kind: Secret
metadata:
  name: {{ include "onekg.fullname" . }}-secrets
  labels:
    app.kubernetes.io/name: {{ include "onekg.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
{{- range $section, $values := .Values.midil.secrets }}
{{ include "onekg.flatten" (dict "root" "MIDIL" "section" ($section | upper) "values" $values) | indent 2 }}
{{- end }}

{{- with .Values.secrets.extraSecrets }}
{{- range $key, $value := . }}
  {{ $key }}: {{ $value | b64enc | quote }}
{{- end }}
{{- end }}
